export const metadata = {
  title: 'Login',
  description: 'Sign in to your Spark FitLife account to access your fitness dashboard and community.',
};

export default function LoginLayout({ children }) {
  return children;
}
