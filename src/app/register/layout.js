export const metadata = {
  title: 'Register',
  description: 'Create your free Spark FitLife account and join the fitness community today.',
};

export default function RegisterLayout({ children }) {
  return children;
}
